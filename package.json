{
  "name": "lexen-offer-sheet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && cp dist/lexen-offer-sheet.js deploy/lexen-offer-sheet.js && cp dist/_headers deploy/_headers",
    "build:lambda": "node scripts/build-lambda.js",
    "dev": "vite"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
